<nav class="navbar-wagon" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">
        <%= link_to seller_account_sellers_home_path do %>
          <%= image_tag "logo_no_house.png" %>
        <% end %>
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if seller_signed_in? %>
          <li>
            <%= link_to "You are logged in as #{current_seller.name}", seller_account_sellers_home_path%>
          </li>
          <li class="dropdown">
            <%= link_to "#", {class: "dropdown-toggle", "data-toggle" => "dropdown", "role" => "button", "aria-expanded" => "false"} do %>
              Access seller account<span class="caret"></span>
            <% end %>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to seller_account_sellers_profile_path do %>
                  <i class="fa fa-user"></i> <%= t ".profile", default: "Seller profile page" %>
                <% end %>
              </li>
              <li>
                <%= link_to seller_account_product_groups_path do %>
                  <i class="fa fa-book"></i> <%= t ".profile", default: "Catalogue" %>
                <% end %>
              </li>
              <li>
                <%= link_to seller_account_orders_path do %>
                  <i class="fa fa-shopping-cart"></i> <%= t ".profile", default: "All orders" %>
                <% end %>
              </li>
              <li>
                <%= link_to destroy_seller_session_path, method: :delete do %>
                  <i class="fa fa-sign-out"></i>  <%= t ".sign_out", default: "Sign out" %>
                <% end %>
              </li>
            </ul>
          </li>
          <li>
            <%= link_to pending_orders_seller_account_orders_path do %>
              <%= image_tag "shop_small.png"%>
            <% end %>
            <div class="image-circle-panier">
              <div class="image-circle-number">
                <%= current_seller.orders.where(status_order: "pending").count %>
              </div>
            </div>
          </li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
